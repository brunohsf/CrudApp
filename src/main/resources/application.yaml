server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_crud?serverTimeZone=ADT
    username: postgres
    password: postdba
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true